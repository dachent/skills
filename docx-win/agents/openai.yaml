interface:
  display_name: "DOCX Win"
  short_description: "native microsoft word automation for windows docx workflows"
  default_prompt: "Use $docx-win to automate a local Word document on Windows with Microsoft Word COM for creation, editing, review, conversion, and PDF export."
