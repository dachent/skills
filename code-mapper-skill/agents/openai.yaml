interface:
  display_name: "Code Mapper"
  short_description: "Map Python dependencies, artifacts, and contracts"
  default_prompt: "Use $code-mapper-skill to map dependency, reference, artifact, and contract relationships before changing this codebase."
