interface:
  display_name: "Repo Map"
  short_description: "Map project structure for planning"
  default_prompt: "Use $repo-map to build a durable project map before planning."

policy:
  allow_implicit_invocation: true
