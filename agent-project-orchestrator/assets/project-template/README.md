# Project template placeholder

This asset documents the planned project-local state layout. It is not the final runtime initializer.

A future `projectctl init` command will create and migrate this structure transactionally.
